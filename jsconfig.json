{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"],
      "@/contracts/*": ["app/contracts/*"], // Add this for contracts directory
      "@/contracts/config": ["app/old/config.js"], // Update to point to contracts/config.js
      "@/app/*": ["./app/*"],
      "@/abi/*": ["./abi/*"] // Add for ABI files
    }
  }
}

